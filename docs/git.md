# Git 사용 기록

이 문서는 Python 퀴즈 게임 프로젝트에서 Git을 사용하며 익힌 기본 흐름과 명령어를 정리한 문서입니다.

## 1. 브랜치 생성 및 전환

```bash
git checkout -b <브랜치이름>
```

```bash
PS [ 프로젝트 폴더 경로 ]> git checkout -b feature/quiz-game
Switched to a new branch 'feature/quiz-game'
```

## 2. 브랜치 확인

```bash
git branch
```
```bash
PS [ 프로젝트 폴더 경로 ]> git branch
* feature/score-percentage
  main
```

## 3. 업스트림 브랜치 설정 및 push

```bash
git push --set-upstream origin <브랜치이름>
```

```bash
PS [ 프로젝트 폴더 경로 ]> git push --set-upstream origin feature/quiz-add
```

## 4. 원격 저장소 복제

```bash
git clone <저장소 주소>
```

## 5. 원격 저장소에서 최신 변경사항 받기

```bash
git pull
```

## 6. 커밋 메시지 규칙

| 유형         | 설명                | 예시                         |
| ---------- | ----------------- | -------------------------- |
| `feat`     | 새로운 기능 추가         | `feat: 퀴즈 삭제 기능 추가`        |
| `fix`      | 오류 수정             | `fix: 퀴즈가 없을 때 실행 오류 수정`   |
| `refact`   | 기능 변경 없는 코드 구조 개선 | `refact: Quiz 클래스 구조 개선` |
| `docs`     | README 및 문서 수정    | `docs: 실행 방법 추가`           |
| `merge`    | 브랜치 병합            | `merge: 퀴즈 추가 기능 병합`       |

커밋 메시지는 `유형: 작업 내용` 형식으로 작성합니다.


## 7. 커밋 기록

| 순서 | 커밋 메시지                      | 작업 내용                                    |
| -: | --------------------------- | ---------------------------------------- |
|  1 | `feat: first commit`        | 프로젝트 생성 및 초기 파일 구성                       |
|  2 | `feat: 공통 입력 처리 구현`         | 숫자 입력을 처리하는 공통 입력 함수 구현                  |
|  3 | `feat: 퀴즈 메인로직 제작`          | 문제 출력, 선택지 출력, 정답 판정 로직 구현               |
|  4 | `feat: 기본 퀴즈 데이터 추가`        | 기본으로 제공되는 수도 퀴즈 데이터 추가                   |
|  5 | `feat: 파일 저장 및 불러오기`        | JSON 파일을 이용한 퀴즈 및 점수 저장 기능 구현            |
|  6 | `feat: 퀴즈게임 실행 및 점수관리`      | 메뉴 반복 실행, 퀴즈 진행, 점수 출력 기능 구현             |
|  7 | `docs: README 개편 및 개발기록 분리` | README를 정리하고 기능별 개발 기록을 별도 문서로 분리        |
|  8 | `feat: 점수 퍼센트에이지 출력`        | 맞힌 문제 수를 백분율 점수로 변환해 출력                  |
|  9 | `docs: branch 병합`           | 기능 브랜치 병합 과정과 명령어 기록                     |
| 10 | `docs: 브랜치명 수정`             | Git 사용 기록에 작성된 브랜치 이름 수정                 |
| 11 | `docs: 브랜치명 수정`             | 문서 내 브랜치 이름과 예시를 추가 수정                   |
| 12 | `feat: 최고점수 확인`             | 이전 플레이의 최고 점수를 저장하고 확인하는 기능 구현           |
| 13 | `merge: 최고점수 확인`            | 최고 점수 기능 브랜치를 `main` 브랜치에 병합             |
| 14 | `docs: 최고점수 기능`             | 최고 점수 저장값과 계산 방법을 개발 기록에 추가              |
| 15 | `refact: 입력 처리 함수 개선`       | 빈 입력, 문자 입력, 범위를 벗어난 숫자를 다시 입력받도록 개선     |
| 16 | `feat: 퀴즈 추가 기능`            | 사용자에게 문제, 선택지, 정답을 입력받아 퀴즈를 추가하는 기능 구현   |
| 17 | `docs: 추가기능 기록`             | 퀴즈 추가 기능과 입력 처리 개선 내용을 문서화               |
| 18 | `merge: 퀴즈 추가 기능`           | 퀴즈 추가 기능 브랜치를 `main` 브랜치에 병합             |
| 19 | `refact: 퀴즈 클래스화`           | 함수로 작성된 퀴즈 처리 로직을 `Quiz` 클래스로 변경         |
| 20 | `feat: 퀴즈 랜덤 출제`            | 저장된 퀴즈의 출제 순서를 무작위로 섞도록 구현               |
| 21 | `feat: 퀴즈 출제 문제 개수 지정`      | 사용자가 풀 문제 개수를 직접 선택하도록 구현                |
| 22 | `feat: 퀴즈 삭제 기능`            | 목록에서 퀴즈를 선택해 삭제하고 변경 내용을 저장하도록 구현        |
| 23 | `feat: 퀴즈 힌트 기능 추가`         | 힌트 출력과 힌트 사용 횟수에 따른 점수 감점 기능 구현          |
| 24 | `feat: 퀴즈 게임 진행 기록`         | 플레이 시간, 정답 수, 힌트 사용 횟수, 점수를 기록하도록 구현     |
| 25 | `docs: 문서 작성`               | 현재까지 구현한 기능과 프로젝트 사용 방법을 문서에 반영          |
| 26 | `fix: 퀴즈 클래스 구조 변경`         | `Quiz` 클래스를 개별 문제 객체로 재구성하고 게임 관리 책임을 분리 |
| 27 | `docs: 리드미 작성`              | 실행 방법, 메뉴, 저장 구조 등을 중심으로 README 수정       |
| 28 | `docs: 클래스 설명 추가`           | 클래스와 함수의 차이 및 프로젝트 클래스 구조 설명 추가          |
| 29 | `docs: git 커밋 메시지 규칙 추가`    | 커밋 유형과 작성 형식을 Git 문서에 추가                 |